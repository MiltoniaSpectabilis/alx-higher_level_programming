-- Select data (score and name)
SELECT score, name
-- From second_table
FROM second_table
-- Where the score is greater than or equal to 10
WHERE (score >= 10)
-- Order data by descending score
ORDER BY score DESC;
